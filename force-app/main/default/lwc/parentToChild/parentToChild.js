import { LightningElement } from 'lwc';

export default class ParentToChild extends LightningElement {
    display = "Hello venkat, ";
    userDetails = {
        name: "Ankit",
        channel: "Tech journey with Ankit"
    }
}